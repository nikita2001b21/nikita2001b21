// WHO ELIGIBLE FOR VOTE

let name1 = 'Akash';
let age = 50;
 
if(age >= 18)
{
    console.log( name1 + " is eligible to vote" );
}
if(age < 18)
{
    console.log(name1 + " is not eligible to vote");
}

